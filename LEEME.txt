CABILDO — F6 + F7 + auditoría de bugs + pulido visual
=====================================================

Cambian 43 archivos: todos dentro de src/ y supabase/.

QUÉ ENTRA
  · F6  Cuotas por ejercicio: salto de año (emisión anual a todo el censo) y
        "simular cobro" de la remesa.
  · F7  Eventos y tareas: calendario, tareas por evento y "trabajadores"
        (quién hace qué), con panel de pendientes por persona.
  · Dos auditorías de bugs (unos 60 hallazgos) y un repaso visual completo en
    tema claro y oscuro. Entre lo corregido: el QR no se podía escanear, al
    imprimir en tema oscuro no se leía nada, un token de CSS inexistente
    borraba los bordes de media interfaz, doble cobro en fraccionamientos
    mensuales y todas las fechas de cobro con un día de desfase.

CÓMO APLICARLO
  Opción A (recomendada), desde la raíz del repo:
      git apply cabildo-todo.patch

  Opción B: copia las carpetas src/ y supabase/ de este zip ENCIMA de las
  tuyas. Respeta las rutas: NADA va en la raíz del repo.

DESPUÉS
      git add -A
      git commit -m "F6 + F7 + auditoría"
      git push

LIMPIEZA (quedan archivos míos sueltos en la raíz de tu repo):
      git rm F4-F5.patch F4-precios.patch LEEME.txt

BASE DE DATOS (solo cuando conectes Supabase)
  supabase/schema.sql        → si creas la base desde cero
  supabase/migracion-2026-08.sql → si ya la tenías creada antes
